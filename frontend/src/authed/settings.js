import React from 'react';
import './profile/customStyle.css';

const Settings = () => {
  return (
    <div>
      Settings
      {console.log('Settings')}
    </div>
  );
};

export default Settings;
